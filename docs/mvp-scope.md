# MVP 范围守卫

首版必须在没有以下组件时完整工作：

- Agent、LLM、向量数据库和自动进度判断。
- GitHub、GitLab、聊天、文档或本地代码采集。
- Neo4j 或其他图数据库。
- Kafka、通用消息队列和微服务拆分。
- 离线写入、outbox、CRDT 或点对点同步。
- 自定义字段、自定义工作流、甘特图和工时核算。
- 键盘、截图、剪贴板、麦克风、私聊或代码正文采集。

`pnpm scope:check` 对已知越界依赖和敏感浏览器 API 做自动检查。范围变更必须先更新 OpenSpec，而不能在实现中静默扩大。
